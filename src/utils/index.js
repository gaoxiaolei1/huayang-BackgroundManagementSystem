const BASE_URL = 'http://api.map.chinaso.com/feeduser/';

const DATAURL = {
  //公共接口
  favorite: `${BASE_URL}tags.json`,
  // Recommended: `${BASE_URL}favorite1/uf`,
  // portrait: `${BASE_URL}favorite2/uf`
};

export default DATAURL;